user id update works now through pipedream. There are a bunch of ux issues that has to be fixed. 
1) I don't want the dashboard to be a floating overlay. Let's keep the dashboard on the framer page directly. 
    Signed in user flow 
    * Do not display results. Instead display the dashboard with the generations presented as a floating overlay over the dashboard list. 
    * A close icon on the top left should allow users to close the results card and return to the dashoboard home - list containing all generations.
    * Remove the prompt component that prompts the user to sign up. This should only be displayed for anon users.
    Anon user flow
    * Display the results just like how we do it currently. Have the prompt to sign up below the results. 

2) Dashboard design 